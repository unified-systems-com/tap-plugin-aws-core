{
  "slug": "ATTACHED_TO_VPC__aws_core",
  "name": "Attached To VPC",
  "description": "An internet gateway is attached to a VPC (gateway -> VPC): ec2:AttachInternetGateway, reported as the gateway's Attachments[].VpcId. Separate from RESIDES_IN_VPC because an internet gateway exists on its own and can be detached and attached elsewhere. The `_TO` is the locative preposition of 'attached to'.",
  "sources": [
    "aws_core__aws_internet_gateway"
  ],
  "targets": [
    "aws_core__aws_vpc"
  ],
  "default_dimensions": {
    "tap.cloud": "aws"
  }
}
