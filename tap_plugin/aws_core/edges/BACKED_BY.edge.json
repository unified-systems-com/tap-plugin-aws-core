{
  "slug": "BACKED_BY__aws_core",
  "name": "Backed By",
  "description": "Service is backed by an underlying resource (ECS service by target group, ALB by ACM cert, etc.).",
  "property_schema": {
    "type": "object",
    "properties": {
      "relationship": {
        "type": "string",
        "description": "Nature of the backing relationship."
      }
    }
  },
  "default_dimensions": {
    "tap.cloud": "aws"
  }
}
