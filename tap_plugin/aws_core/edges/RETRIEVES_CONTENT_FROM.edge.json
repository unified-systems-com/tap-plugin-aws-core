{
  "slug": "RETRIEVES_CONTENT_FROM__aws_core",
  "name": "Retrieves Content From",
  "description": "A CloudFront distribution retrieves origin content from an S3 bucket. The distribution initiates the pull (edge direction); content data flows from the bucket back to the distribution — a data-backwards edge, which the `_FROM` marks.",
  "sources": [
    "aws_core__aws_cloudfront_distribution"
  ],
  "targets": [
    "aws_core__aws_s3_bucket"
  ],
  "default_dimensions": {
    "tap.cloud": "aws"
  }
}
