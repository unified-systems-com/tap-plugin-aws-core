{
  "slug": "INVOKES__aws_core",
  "name": "Invokes",
  "description": "A service invokes a Lambda function (e.g. an EventBridge rule targeting a Lambda). Source is open because several service types can invoke a Lambda; the target is the invoked Lambda. This is the only invocation relationship the collector emits today.",
  "targets": ["aws_core__aws_lambda"],
  "property_schema": {
    "type": "object",
    "properties": {
      "method": {
        "type": "string",
        "description": "Invocation method or API action."
      }
    }
  },
  "default_dimensions": {
    "tap.cloud": "aws"
  }
}
