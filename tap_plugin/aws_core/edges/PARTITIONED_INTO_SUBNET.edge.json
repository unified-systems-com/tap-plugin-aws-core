{
  "slug": "PARTITIONED_INTO_SUBNET__aws_core",
  "name": "Partitioned Into Subnet",
  "description": "A VPC is partitioned into a subnet (VPC -> subnet): the subnet's VpcId. A subnet exists only inside its VPC, so the VPC declares this edge in CONTAINMENT_EDGES and retiring a VPC retires its subnets.",
  "sources": [
    "aws_core__aws_vpc"
  ],
  "targets": [
    "aws_core__aws_subnet"
  ],
  "default_dimensions": {
    "tap.cloud": "aws"
  }
}
